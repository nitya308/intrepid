import React from 'react';
import {
    StyleSheet, View, Text, Image,
} from 'react-native';

const UserRank = (props) => {
    return (
        <View>
        </View>
    )
}

export default UserRank;